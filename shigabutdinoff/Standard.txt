// Finds whether a variable is an array
func is_array(value)
    return Type(value) = Type([])

// Finds whether a variable is null.
func is_null(value)
    return Type(value) = Type(null)

// Create an array containing a range of elements
func range(from, to)
    var values = []
    for value = from..to
        values.Add(item)
    return values

func array_values(values)
    var array = []
    for value : values
        array.Add(value)
    return array